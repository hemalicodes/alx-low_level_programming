#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Str[] = ("with proper grammar, but the outcome is a piece of art,\n");
	printf(Str);
	return (0);
}
