#include <stdio.h>
#include <stdlib.h>
#include <lists.h>
/**
 * listint_len - Returns the number of elements in  a linked listint_t list
 * @h: input
 *
 *Return: number of elements
kk */
size_t listint_l
{
size_t num = 0;
while(h)
{
num++;
h = h -> next;
}
return(num);
}
