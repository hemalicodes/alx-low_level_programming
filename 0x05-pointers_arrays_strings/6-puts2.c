#include "main.h"
#include <stdio.h>
/**
 * puts2 - prints all char of a string
 *
 * @str: input
 *
 * Return: Always 0
 */
void puts2(char *str)
{
int i, j; 
for (i = 0; str[i] != '\0' ; i++)
{
;
}
for (j = 0; j < i; j = j + 2)
{
putchar(str[j]);
}
putchar('\n');
}
