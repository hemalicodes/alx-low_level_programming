#include<stdio.h>
#include "main.h"
/**
 * main -check the code
 *
 * c - char input
 *
 * Return: 1 or 0 Always
 */

int main(void)
{
char c;
if( c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
