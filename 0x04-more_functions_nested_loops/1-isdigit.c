#include "main.h"
/**
 * isdigit -  function that checks for a digit (0 through 9)
 *
 * @cd - char input
 *
 * Return: Returns 1 if c is a digit and 0 when not
 */
int _isdigit(int c)
{
if((c >= '0') && (c <= '9')
return (1);
else
return (0);
}
